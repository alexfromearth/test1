


const initialState = {
  taskList: null,
  isFetching: false,
}


const mainReducer = (state = initialState,  action) => {
  switch (action.type) {
    default:
      return state;
  }
}


export default mainReducer;
